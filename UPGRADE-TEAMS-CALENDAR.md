# HourLedger — שדרוג "צוות + יומן": תדריך לפתיחת סשן

נכתב 18.9.2026 בסוף סשן הדמו לוובינר. המסמך הזה הוא נקודת הפתיחה לסשן חדש.
לפני שמתחילים לקרוא גם: [HANDOFF.md](HANDOFF.md) (מצב האפליקציה), `supabase/schema.sql`,
והזיכרון `hourledger-demo-setup` / `hourledger-accounts-map`.

## 1. מה בונים

שני פיצ'רים, בסדר הזה:

**A. צוות (multi-user במשרד)** — כמה עורכי דין ומתלמדים באותו משרד, לקוחות משותפים,
דיווחים אישיים, היררכיה ראשי → עו״ד → מתלמד, דשבורד שמרכז הכל.

**B. סנכרון יומן** — היומן של המתלמד מכתיב מה צריך לדווח; פאנל "מהיומן היום" עם דיווח
בלחיצה, ותזכורת יומית על פערים.

## 2. החלטות שכבר התקבלו (לא לפתוח מחדש)

- עו״ד רגיל רואה **וגם מתקן** את דיווחי המתלמדים שתחתיו. מתלמד רואה רק את עצמו. ראשי — הכל.
- **תעריף אחיד** ללקוח (לא לפי תפקיד). אין שינוי בטאב `תעריפים`.
- דיווח הוא תמיד של מדווח אחד. אין "דיווח משותף"; הדשבורד מאחד.
- לקוחות (שם, ח״פ, אימייל) משותפים לכל המשרד.
- הפקת חשבונית, מחיקת לקוח, ניהול צוות — רק ראשי.
- היומן **לא** מדווח אוטומטית — רק מציע; המתלמד מאשר בלחיצה.
- **אורן לא רואה שינוי.** כל דבר חדש מאחורי דגל ב-`FIRM` (כמו `invoicing`): `FIRM.teams`,
  `FIRM.calendar`. אתר Law: `false`. מפתחים ובודקים על **הדמו** (`HourLedger-Demo`).
- קודם Make + אימייל, לא OAuth אישי לכל מתלמד (חיבור יומן אחד של המשרד).

## 3. מצב קיים שצריך להכיר

- קובץ אחד: `index.html` (vanilla JS, localStorage + Supabase). שני ריפו זהים למעט בלוק `FIRM`
  (`Waives-io/HourLedger-Law`, `Waives-io/HourLedger-Demo`). כל שינוי נעשה בשניהם
  (Law עם הדגלים כבויים). לבדיקה מקומית: `.claude/launch.json` → `hourledger` (py http.server 8765).
- Supabase: `orgs` → `profiles` (user_id, org_id, email) → `clients` / `categories` / `records`.
  `records` כבר נושא `user_email`. RLS: כל חבר משרד רואה ועורך **הכל** במשרד (`my_org()`).
  **כל הרשמה פותחת משרד חדש** (`handle_new_user`) — אין היום דרך להצטרף למשרד קיים.
- הדשבורד: חיתוכים לפי לקוח / תאריך / קטגוריה, טווח תאריכים, חיפוש. `subTreeHTML` בונה את
  ההיררכיה הפנימית. `state.dashMode` נשמר בהעדפות.
- Make (WAIVES, סצנריו 7442454, hook `we3jdwllve7z9ei9zn18i8c8ls1rng6x`): מסלולים
  create/update/delete/invoice/invoice_preview/client. הגיליון: עמודה M = משתמש, P = ח״פ.
  תוכנית Free (2 סצנריו פעילים, 1000 פעולות/חודש) — סצנריו יומן מתוזמן = סצנריו נוסף → Core.
- מצב מקומי (בלי ענן) לא תומך בצוות. צוות = חובה Supabase.

## 4. ארכיטקטורה מוסכמת

### A. צוות

**סכימה**
- `profiles`: + `role` (`owner` | `lawyer` | `trainee`), + `supervisor_id` (uuid → profiles.user_id, למתלמד), + `display_name`.
- `invites`: `code`, `org_id`, `email`, `role`, `supervisor_id`, `created_by`, `used_at`.
- `records`: + `user_id` (uuid). `user_email` נשאר לתאימות ולגיליון.
- `handle_new_user`: אם ב-metadata יש `invite` תקף → מצרף למשרד הקיים ומסמן את ההזמנה; אחרת כמו היום.

**RLS** — פונקציה `can_see(record_user_id)`:
`owner` → הכל; `lawyer` → שלו + מי ש-`supervisor_id` שלו הוא; `trainee` → שלו. אותה פונקציה ל-select ול-update/delete.
`clients`/`categories`: קריאה לכולם, כתיבה ל-owner (ועו״ד? — ברירת מחדל owner בלבד).

**אפליקציה**
- הגדרות → "צוות" (רק owner): רשימת חברים + תפקיד + אחראי, יצירת הזמנה (קוד/קישור), הסרה.
- הרשמה עם קוד הזמנה (`?invite=…` בקישור ממלא אוטומטית).
- טופס דיווח: ללא שינוי (המדווח = המשתמש המחובר).
- דשבורד: מסנן "מי דיווח" (הכל / אני / הצוות שלי / אדם) — מוסתר למתלמד; חיתוך "לפי עורך דין";
  בכל חיתוך רמת עו״ד בהיררכיה (לקוח → עו״ד → סוג חיוב).
- `payloadFor`: + `userName`, `userRole`. Make/גיליון: עמודה חדשה לתפקיד (או להשאיר M בלבד).
- מסך "הדיווחים האחרונים" — של המשתמש עצמו.

### B. יומן

- חיבור אחד: יומני המתלמדים משותפים לחשבון המערכת של המשרד (Google Workspace / 365) → חיבור אחד ב-Make.
- זיהוי לקוח: שם הלקוח בכותרת האירוע (התאמה מטושטשת מול רשימת הלקוחות) או יומן/צבע "לחיוב".
  לא זוהה → מופיע באפליקציה כ"בחר לקוח", המיפוי כותרת→לקוח נשמר (`calendar_map` ב-Supabase).
- טבלה `calendar_events` (org_id, user_id, event_id, date, title, minutes, client_guess, reported_record_id).
- סצנריו Make מתוזמן (יומי, למשל 18:00): אירועי היום לכל יומן → כתיבה ל-`calendar_events` →
  השוואה לדיווחים (משתמש + תאריך + לקוח) → פערים → אימייל למתלמד עם קישור עמוק. שבועי: תקציר לאחראי.
- אפליקציה: פאנל "מהיומן היום/אתמול" בראש מסך הדיווח, סטטוס ✓/⏳, "דווח" פותח טופס ממולא
  (תאריך, לקוח, שעות = משך האירוע). פתיחה מקישור: `?date=&client=&hours=&event=`.
- שדרוגים אחרי: WhatsApp דרך Make, Push של ה-PWA.

## 5. שלבים והערכות

| # | שלב | זמן |
|---|---|---|
| A1 | סכימה + RLS + הזמנות + הרשמה עם קוד + מסך "צוות" | 3–4 ש׳ |
| A2 | נראות בדשבורד, מסנן "מי דיווח", חיתוך לפי עו״ד, הסתרה למתלמד | 2–3 ש׳ |
| A3 | payload/גיליון: שם ותפקיד מדווח; בדיקות עם 3 חשבונות; HANDOFF + סקיל | 2 ש׳ |
| B1 | אפליקציה: פתיחה ממולאת מקישור, פאנל "מהיומן", זכירת מיפוי | 3–4 ש׳ |
| B2 | Make: סצנריו יומי יומן → השוואה → Supabase → אימייל | 2–3 ש׳ |
| B3 | תקציר שבועי לאחראי, WhatsApp/Push (אופציונלי) | 2 ש׳ |

סה״כ ≈ יום–יום וחצי ל-A, ≈ יום ל-B. לפצל לסשנים לפי שלב, בדיקה של גלי אחרי כל שלב.

## 6. הוחלט 18.9.2026 (בפתיחת סשן השדרוג)

1. יומן: **Google Calendar** (Outlook אולי בהמשך).
2. סימון אירוע לחיוב: **שם הלקוח בכותרת האירוע** (התאמה מטושטשת לרשימת הלקוחות; לא זוהה → "בחר לקוח" + זכירת מיפוי).
3. תזכורת יומית: **אימייל ב-18:00** (Make). WhatsApp/Push אחר כך.
4. הוספת לקוחות: **רק ראשי (owner)**. עו״ד ומתלמד בוחרים מהרשימה בלבד.

## 7. מצד גלי לפני שמתחילים

- להריץ SQL ב-Supabase (יינתן בשלב A1). לפתוח 3 חשבונות בדיקה בדמו (ראשי / עו״ד / מתלמד).
- לשקול Core ב-Make (סצנריו יומן + הדמו + Leads + תזכורות הוובינר).
- ליומן: יומן בדיקה אחד משותף לחשבון contact@waives.io.

## 8. עדיין פתוח מהסשן הקודם (לא קשור, לא לשכוח)

- ניקוי: חשבוניות בדיקה ב-Zoho (INV-000001…13), לקוח Zoho "לקוח דמו א'" בלי ח״פ, שורות בדיקה
  בגיליון (`תעריפים`: 500000001, 500000002; DB: שורות "בדיקה — למחוק" מ-20/08/2026).
- להזין ח״פ + אימייל ללקוחות הדמו בהגדרות, ריצה מלאה מהאתר (תצוגה → אישור → מייל).
- Make ל-24.9: Core לחודש או החלפה ידנית; **להפעיל את תזכורת 10:00 לפני 24.9 בבוקר.**

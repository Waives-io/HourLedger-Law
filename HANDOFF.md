# HourLedger-Law — מסמך העברה ל-Claude Code

מסמך אחד שמסכם את מצב הפרויקט, ההחלטות שהתקבלו, ומה פתוח. להעלות לריפו יחד עם שאר הקבצים כדי ש-Claude Code יקרא אותו בתחילת כל סשן.

---

## מה זה

אפליקציית דיווח שעות חיוב למשרדי עורכי דין. נבנתה עבור משרד עו"ד אורן וינברג, אבל מותאמת ממשרד ראשון להיות מוצר לכמה משרדים. HourLedger-Law היא הגרסה למשרדי עו"ד; בהמשך ייתכנו גרסאות HourLedger אחרות לסוגי משרדים אחרים.

- ריפו: `Waives-io/HourLedger-Law`
- אתר חי: https://waives-io.github.io/HourLedger-Law/
- קבצים: `index.html` (כל האפליקציה), `logo.png`, `icon-192.png`, `README.md`

## ארכיטקטורה

קובץ HTML יחיד, vanilla JS, ללא build ו-ללא תלויות חיצוניות מלבד גופנים מ-Google Fonts. אין שרת. הנתונים נשמרים ב-localStorage ומשוכפלים לגוגל שיטס דרך webhook של Make.

**מודל הנתונים — חשוב:** רשומה אחת = שורת חיוב אחת. דיווח עם שלוש קטגוריות יוצר שלוש רשומות, כל אחת עם `id` משלה ו-`groupId` משותף, אותו לקוח, אותו תאריך, שעות נפרדות, והערות משוכפלות. זו הדרישה העסקית המרכזית ואין לשנות אותה.

```js
{ id, groupId, date, client, category, hours, notes, createdAt, updatedAt }
```

**סנכרון:** כל create / update / delete נשלח ל-
`https://hook.eu1.make.com/3f461zbp81thyc76u95jzfs3yn6i5p1r`
עם `action`, `id`, `groupId`, `date`, `client`, `category`, `categoryId`, `hours`, `notes`, `user`, `org`, `orgName`, `source`, `appVersion`, `timestamp`.
ב-Make: Router לפי `action` — create מוסיף שורה, update עושה Search Rows לפי `id` ואז Update, delete מוחק. כישלון שליחה נכנס ל-outbox מקומי, מוצג כ"ממתין לסנכרון" בכותרת, ונשלח שוב אוטומטית. יש נפילה ל-`text/plain` אם preflight של CORS נחסם.

**חשבונות ונתונים בענן (Supabase):** מגרסה 4.0 החשבונות והנתונים (דיווחים, לקוחות, קטגוריות) נשמרים ב-Supabase — כניסה מכל מכשיר, שחזור סיסמה במייל. הסכמה ב-`supabase/schema.sql` (טבלאות `orgs`, `profiles`, `clients`, `categories`, `records` + RLS לפי משרד; טריגר על `auth.users` פותח משרד חדש לכל נרשם). המפתחות `SUPABASE_URL` / `SUPABASE_KEY` בראש הסקריפט ב-`index.html`; כשהם ריקים האפליקציה רצה במצב "מכשיר בלבד" (ההתנהגות הישנה). localStorage נשאר מטמון + תור כתיבה (`hourledger.cloudout.v1.<org>`) לעבודה ללא רשת; בכניסה מושכים את העותק מהענן. **קוד הכניסה המהיר** (4–6 ספרות) נשאר מקומי למכשיר — hash ב-`hourledger.accounts.v1` עם `cloud:true`. "נעילה" = דורש קוד בפעם הבאה; "התנתקות" = signOut מלא מהמכשיר. חשבון הדמו `demo@hourledger.app` / `Demo2026` נשאר מקומי בלבד. דיווחים שנשמרו במכשיר לפני המעבר לענן מוצעים לייבוא בכניסה הראשונה (או מכפתור בהגדרות).

**אתר אחד = משרד אחד (מ-15.9.2026):** הקבוע `FIRM` בראש הסקריפט ב-`index.html` נושא את שם המשרד וה-webhook של הפריסה הזו. כשיש שם, ההרשמה לא מציגה שדה "שם המשרד" וכל חשבון שנפתח באתר שייך למשרד הזה. הפריסה הזו (`HourLedger-Law`) = עו"ד אורן וינברג. משרד חדש = ריפו חדש מהתבנית + שינוי `FIRM` + Pages (סקיל `hourledger-new-office`). Supabase נשאר פרויקט אחד לכולם. קוד הכניסה המהיר = בדיוק 4 ספרות.

**לכל משרד (org):** רשימת לקוחות, רשימת קטגוריות חיוב, webhook. ה-webhook הוא **פר משרד** — כל משרד מקבל תרחיש Make משלו (Clone של `make/HourLedger-Sheets.blueprint.json`) וגיליון משלו. **מ-16.9.2026: כש-`FIRM.webhook` מוגדר בקוד הוא סופי** — `orgCfg()` תמיד מחזירה אותו, שדה ה-webhook בהגדרות מוסתר, וערך שנשמר ב-Supabase או במכשיר לא יכול לדרוס אותו. שדה ההגדרות מוצג רק בפריסה בלי webhook בקוד. משרד חדש מתחיל בלי webhook (סנכרון "לא מוגדר"). רק המשרד המובנה `weinberg` נושא את הכתובת של אורן בקוד (`WEINBERG_WEBHOOK`). ב-Supabase הכתובת נשמרת ב-`orgs.webhook`; במצב מכשיר-בלבד ב-`hourledger.webhook.v1.<org>` / `hourledger.orgs.v1`. מגבלת Make Free: 2 תרחישים פעילים, 1,000 פעולות/חודש — לכל משרד נוסף נדרש חשבון Make משלו (כך הוחלט). רשימת הלקוחות מתחילה ריקה; המשתמש מזין או מדביק אותה בהגדרות. רשימת הקטגוריות מגיעה עם ברירת מחדל גנרית לעו"ד (שיחת טלפון, דיון בבית משפט, גישור, פישור/בוררות, עיון במסמכים, כתיבה/עריכת מסמכים, פגישה, פגישת ייעוץ, פעולה נוטריונית, אחר) וניתנת לעריכה.

**מפתחות localStorage:** `hourledger.accounts.v1`, `hourledger.orgs.v1`, `hourledger.last.v1`, `hourledger.prefs.v1.<email>`, `moneh.records.v2.<org>`, `moneh.clients.v2.<org>`, `moneh.cats.v2.<org>`, `moneh.outbox.v2.<org>`, `moneh.session.v2`, `moneh.lang.v2`. הקידומת `moneh` היא שריד משם קודם — לא לשנות בלי מיגרציה, יש נתוני בדיקה קיימים.

## עיצוב

מבוסס על זהות המשרד: לבן, כחול עמוק `#1F4066`, כחול פלדה `#7C96B5`, זהב `#B7995B`. גופנים: Assistant לטקסט, Cormorant Garamond ללוגוטייפ. אין להמציא גוונים נוספים — כל האפור נגזר מ-rgba של הכחול העמוק. RTL כברירת מחדל עם מעבר לאנגלית שמחליף גם את כיוון העמוד. הלוגו (שעון עם נוצת זהב) מוטמע כ-base64 בתוך `index.html`.

## כללי עבודה

- מובייל תחילה, אבל שיעבוד גם בדסקטופ.
- Tab עובר בין השדות לפי הסדר: תאריך → לקוח → קטגוריות → שורות שעות → הערות. הקטגוריות הן תחנת Tab אחת עם ניווט בחצים.
- שעות תמיד ברבעי שעה.
- תאריך ברירת מחדל = היום, גם אחרי שמירה.
- כל טקסט חדש נכנס לשני מילוני `I18N` (he ו-en).
- אחרי כל שינוי מהותי: לבדוק כניסה, שמירת דיווח מרובה-קטגוריות, עריכה, מחיקה, מעבר שפה.

## מה פתוח

1. **תעריף שעתי ללקוח** — נדחה במפורש (תעריפים בשקל/דולר, לא רוצים סכומים באפליקציה). הדשבורד מסכם שעות בלבד: לפי לקוח / תאריך / קטגוריה, טווח תאריכים, ופירוט משנה בכל קבוצה.
2. **דשבורד משותף** — כרגע כל משתמש רואה רק את הדיווחים שלו. קריאה חזרה מהגיליון דורשת webhook שני ב-Make שמחזיר רשומות.
3. **Supabase** — מחובר ופעיל מ-15.9.2026. פרויקט `HourLedger-Law` (ארגון Waives, Frankfurt), `https://aqzkmmqhaxdyolgrjfnv.supabase.co`. הסכמה הורצה, אימות מייל פעיל, Site URL = כתובת GitHub Pages. נבדק: הרשמה + אימות מייל, כניסה ממכשיר שני עם משיכת נתונים, עריכה/מחיקה, נעילה + קוד. חשבון בדיקה: `gallile777+hltest@gmail.com` (משרד "משרד בדיקה ענן") — למחוק כשלא צריך (Authentication → Users). משתמש לא-מאומת `ulalaoya+hltest@gmail.com` נוצר בטעות בבדיקה — למחוק.
4. **בדיקות בשטח** — הרצה של גלי ושל אורן במקביל, כולל אימות שהשורות נוחתות נכון בגיליון.

---

## הפרומפט לפתיחת סשן ב-Claude Code

> אני עובדת על HourLedger-Law, אפליקציית דיווח שעות למשרדי עו"ד. קרא את HANDOFF.md ואת README.md בריפו לפני שאתה נוגע בקוד. כל האפליקציה נמצאת ב-index.html. אחרי כל שינוי, עשה commit עם הודעה קצרה בעברית ו-push ל-main, ותוודא שגיטהב פייג'ס מתעדכן.
